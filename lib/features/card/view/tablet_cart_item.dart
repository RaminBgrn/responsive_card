import 'package:flutter/material.dart';

class TabletCartItem extends StatelessWidget {
  const TabletCartItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
