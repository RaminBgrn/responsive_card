import 'package:flutter/material.dart';

class DesktopCartItem extends StatelessWidget {
  const DesktopCartItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
